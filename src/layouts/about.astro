---
import Layout from "@layouts/Layout.astro";
import Card from "@components/Card.astro";
import { Icon } from 'astro-icon/components'

import { dropbox_link } from '../constants/socials'

const {frontmatter} = Astro.props;
---

<Layout title="Recharged - Über uns">
  <main>
      <div class="home-card bg-secondary text-white py-3">
          <Card title={frontmatter.title}>
            <a href={dropbox_link} class="flex flex-row items-center justify-center content-center" target="_blank" rel="noopener noreferrer">
              <Icon name="mdi:dropbox" class="sm:w-11 w-7 size-full"></Icon>
              <div class="flex justify-center content-center">
                <h2 class="px-4 sm:text-2xl text-lg text-sans">Werbematerial</h2>
              </div>
            </a>
          </Card>
          <div class="w-full flex justify-center">
            <div class="flex justify-items-center p-6 min-h-screen sm:max-w-6xl">  
              <div class="text-justify text-lg leading-relaxed sm:text-2xl sm:leading-loose">
                <slot/>
              </div>
            </div>
          </div>
        </div>
  </main>
</Layout>

<style>
    .home-card {
      justify-content: center;
      text-align: center;
      width: 100%;
    }
  
    h2, button {
      letter-spacing: 0.17em;
    }
  </style>